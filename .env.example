# Port the server listens on (Cloud Run sets this automatically)
PORT=3001

# Absolute path to the directory where NeDB files are stored.
# On Cloud Run, point this at a mounted volume (e.g. Cloud Storage FUSE or a persistent disk).
# Example: /mnt/disks/data
DB_PATH=/mnt/disks/data
